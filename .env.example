# Langfuse
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# LLM provider used for LLM-as-judge evaluators
LLM_PROVIDER_API_KEY=
LLM_PROVIDER_BASE_URL=

# Database (experiment/run metadata only — datasets live as versioned files, see PROJECT_PLAN.md §4)
DATABASE_URL=postgresql+asyncpg://eval:eval@localhost:5432/ai_eval

# Never commit real values for the above — this file documents required vars only.
